import type { EnDict } from './en';
import { en } from './en';

// Placeholder: copies EN values. TypeScript enforces key/shape parity via the EnDict annotation.
// Translation values will be filled in by a follow-up PR (see issue #64). Do not change keys here without changing en.ts.
export const fr: EnDict = en;
